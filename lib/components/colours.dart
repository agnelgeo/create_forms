import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF874590);
  static const Color secondary = Color(0xFFD1C4E9);
  static const Color background = Color(0xFFFFFFFE);
  static const Color button = Colors.orange; // Orange for buttons
  static const Color footerBackground = Color(0xff00246B); // Dark blue for footer
  static const Color textColor = Color(0xFF874590); // Same as primary for text
  static const Color iconColor = Colors.white; // For white icons
}
